% Example: Calculate parity bits for a vector of integers
inputVector = [170, 85, 42, 127];
result = calculateParityBits(inputVector);

% Display the result
disp('Parity bits for each column:');
disp(result);
